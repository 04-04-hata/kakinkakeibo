<nav class="navbar-light fixed-top">
  <div class="left">
    <a class="navbar-brand"><%= link_to '課金家計簿', root_path %></a>
  </div>
  <div class="right">
    <% if user_signed_in? %>
    <div class="user_name">
      <%= current_user.name %>様
    </div>
      <a class="traffic"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete  %></a>
    <% else %>
      <a class="traffic"><%= link_to 'ログイン', new_user_session_path %></a>
      <a class="traffic"><%= link_to '新規登録', new_user_registration_path %></a>
    <% end %>
    <% if user_signed_in? %>
        <div class="search">
            <%= form_with url: search_path, method: :get, local: true do |f| %>
              目的の物を入力して下さい<%= f.text_field 'search[value]', :placeholder => "例:エレシュキガル" %>
              <%= f.select 'search[model]', options_for_select({"ガチャ家計簿" => "gachakakeibo", "その他家計簿" => "otherkakeibo"}) %>
              <%= f.submit :"検索" %>
            <% end %>
        </div>
    <% end %>
    <div class="dropdown">
      <button class="btn btn-danger dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        Menu
      </button>
      <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
        <% if user_signed_in? %>
          <button class="dropdown-item" type="button"><%= link_to "その他家計簿", other_kakeibos_path %></button>
          <button class="dropdown-item" type="button"><%= link_to "ガチャ家計簿", gacha_kakeibos_path %></button>
          <button class="dropdown-item" type="button"><%= link_to "その他家計簿記述", new_other_kakeibo_path %></button>
          <button class="dropdown-item" type="button"><%= link_to "ガチャ家計簿記述", new_gacha_kakeibo_path %></button>
          <button class="dropdown-item" type="button"><%= link_to "家計簿一覧", kakeibos_path %></button>
          <button class="dropdown-item" type="button"><%= link_to "家計簿登録", new_kakeibo_path %></button>
          <button class="dropdown-item" type="button"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete  %></button>
        <% else %>
          <button class="dropdown-item" type="button"><%= link_to '新規登録', new_user_registration_path %></button>
          <button class="dropdown-item" type="button"><%= link_to 'ログイン', new_user_session_path %></button>
        <% end %>
      </div>
     </div>
  </div>

</nav>
